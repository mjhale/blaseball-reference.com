import styled from "@emotion/styled";

/* @TODO: Add heading levels */

const StyledHeading = styled.h1`
  font-size: var(--fontSans-lg);
  font-weight: var(--fontWeightSemibold);
`;

export default StyledHeading;
